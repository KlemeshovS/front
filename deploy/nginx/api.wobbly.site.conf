server {
    server_name api.wobbly.site;

    location /api/assets/ {
        alias /opt/wobbly-front-production/current/assets/;
        try_files $uri =404;
    }

    location /api/og/ {
        alias /opt/wobbly-front-production/current/og/;
        try_files $uri =404;
    }

    location = /api/docs {
        root /opt/wobbly-front-production/current;
        try_files /index.html =404;
    }

    location / {
        limit_req zone=api_per_ip burst=30;
        limit_conn api_conn_per_ip 20;

        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    listen [::]:443 ssl; # managed by Certbot
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/api.wobbly.site/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/api.wobbly.site/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}

server {
    if ($host = api.wobbly.site) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    listen 80;
    listen [::]:80;
    server_name api.wobbly.site;
    return 404; # managed by Certbot
}
